name: Newman API Tests
on: 
  workflow_dispatch:
jobs: 
  test: 
    defaults: 
      run: 
        working-directory: ./tests
    environment: staging
    name: "Run tests"
    runs-on: ubuntu-latest
    steps: 
      - 
        name: "Run tests"
        run: "newman run DecReq.postman_collection.json ReqRes Dev.postman_environment.json -r htmlextra — reporter-htmlextra-export htmlreport.html  > runreport1.html"

name: Newman API Tests
on: 
  workflow_dispatch:
jobs: 
  test: 
    # defaults: 
    #   # run: 
    #   #   working-directory: test
#    environment: staging
    name: "Run tests"
    runs-on: ubuntu-latest
    steps: 
      - name: "Print working directory"
        run: |
         pwd
      - name: "Run tests"
        run: "newman run postman_collection.json -e postman_env.json -r htmlextra --reporter-htmlextra-export htmlreport.html --verbose > runreport1.html
"

